import { expect } from 'chai'
import * as FetchMock from 'fetch-mock'

import observe from './util/observe-actions'
import <%= functionName %> from './<%= fileBase %>'

describe('<%= functionName %>', () => {
  it('should ', () => {
    
  })
})
