/** <%= description %> */
export interface State {
  
}

export type Action
= { kind: undefined }

const init = {
  
}

export function update(state: State = init, action: Action): State {
  return state
}
