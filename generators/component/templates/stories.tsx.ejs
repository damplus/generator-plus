import * as React from 'react'
import { storiesOf } from '@kadira/storybook';

import <%= componentName %> from './src'
const theme = require('./default.sass')

storiesOf('<<%= componentName %> />', module)
  .add('Initial state', () => (
    <<%= componentName %>
      theme={theme}
    />
  ))
